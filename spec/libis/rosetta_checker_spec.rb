RSpec.describe Libis::RosettaChecker do
  it "has a version number" do
    expect(Libis::RosettaChecker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
